

## img元素的属性

```js
let img = document.imges[0] // 第一个img元素

img.src='https://image-url.png'
```

## a元素的属性

```js
let a = document.imges[0] // 第一个超链接元素

img.href='https://url.html'
```


## button元素的属性

```html
<body>
    <button>点我</button>
    <script>   
        let btn = document.querySelector('button') // 第一个超链接元素
 
        btn.onclick=function (){
            alert('hello')
        }
        
        </script>
</body>
```


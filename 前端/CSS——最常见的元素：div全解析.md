
div可以说是最最常见的元素了，它虽然没有语义，但是在排版布局中必不可少，有时候也可以当做p、img元素使用。

## div的宽度和高度

div的宽度和高度默认继承自父元素。

如果都没有设置，宽度是父元素的宽度，与width:100%等效。而高度由内容撑起来。

如果只设置了宽度，那么高度由内容撑起来。与height:auto。

如果只设置了高度，由于div为块元素，必须占据一行，宽度是父元素的宽度，与width:100%等效。

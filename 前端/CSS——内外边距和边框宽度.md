

一个容器元素例如div通常分为：
- margin	外边距
- border	边框
- padding	内边距

对于四个方向上的长度属性，包括内外边距、边框宽度，可以设置4个、3个、2个、1个值，这四种情况代表的含义如下：

- 设置4个值： 依次为上、右、下、左，顺时针旋转。
- 设置3个值：依次为上、左右、下。这点容易忘记。
- 设置2个值：依次会上下、左右。
- 设置1个值：四周采用相同的值。

